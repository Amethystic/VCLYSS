    # ← paste the content from above or use curl method

# 3. Very important: remove already tracked garbage from git index
#    (this is the step most people forget → gitignore won't work on already added files!)

# Remove the big folders (they will be re-generated by Unity anyway)
git rm -r --cached obj
git rm -r --cached Library
git rm -r --cached Temp
git rm -r --cached .vs               # Visual Studio cache
git rm -r --cached Builds            # if you have it
git rm    --cached *.csproj          # project files (Unity regenerates them)
git rm    --cached *.sln

# If you have many small files you want to clean (be careful!)
# git rm -r --cached .                 # ← dangerous — only if you know what you're doing

# 4. Now commit the removal + new .gitignore
git add .gitignore
git commit -m "Add proper .gitignore + remove generated folders/files from tracking"

# 5. (Optional but recommended) Force clean status
git status
